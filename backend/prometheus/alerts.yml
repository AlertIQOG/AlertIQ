groups:
  - name: alertiq-example-rules
    rules:
      - alert: AlwaysFiringExample
        expr: vector(1)
        for: 30s
        labels:
          severity: warning
          app: alertiq-demo
          component: prometheus
          region: local
        annotations:
          summary: "Example alert for AlertIQ ingest testing"
